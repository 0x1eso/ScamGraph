# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Celery beat 런타임 산출물(스케줄 상태 파일)
celerybeat-schedule*
celerybeat.pid

# 배포 산출물 — 대용량 이미지 번들 · 실제 터널 크리덴셜(예시 파일만 커밋)
deploy/scamgraph-images.tar*
deploy/cloudflared/config.yml
deploy/cloudflared/*.json

# Java / Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Env / secrets
.env
.env.local
*.local

# OS / editor
.DS_Store
.idea/
.vscode/
*.log

# Docker volumes (root-level bind mounts only — not app source like apps/web/app/data)
/data/

# 엔진(Python) — 이제 저장소에 편입(직접 관리)

CLAUDE.md
.claude