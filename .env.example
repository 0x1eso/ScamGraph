# --- Postgres ---
POSTGRES_USER=scamgraph
POSTGRES_PASSWORD=scamgraph
POSTGRES_DB=scamgraph

# --- Neo4j ---
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=scamgraph123

# --- Redis (Celery broker) ---
REDIS_URL=redis://localhost:6379/0

# --- Meilisearch ---
MEILI_URL=http://localhost:7700
MEILI_KEY=scamgraph_dev_key

# --- Engine ---
ENGINE_URL=http://localhost:8000

# --- Web ---
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080

# --- AI 2차 소견 (선택 · 참고용) ---
# 규칙 판정과 별개인 부가 계층. 비워 두면 게이트웨이가 호출 없이 안전 스킵(available:false).
# 활성화하려면 GLM_API_KEY 에 Zhipu(BigModel) 키를 넣고 gateway 를 재기동한다.
GLM_API_KEY=
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
GLM_MODEL=glm-5.2
